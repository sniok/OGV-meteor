sudo: false
 
language: node_js
 
before_install:
  - curl https://install.meteor.com | /bin/sh
  - export PATH="$HOME/.meteor:$PATH"
 
branches:
  only:
    - eslint
cache:
  directories:
    - node_modules
 
before_script:
  - cd OGV
  - meteor npm install
 
script:
  - meteor npm run lint